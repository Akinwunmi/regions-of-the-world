import { RegionNameToUrlPipe } from './region-name-to-url.pipe';

describe('RegionNameToUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new RegionNameToUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
